import React from "react";

const Header = () => {
    return (
        //(we need to render that on the home page, home component)
        <div>
                <h1 className="font-weight-light display-1 text-center"> Restaturants Finder</h1> 
        </div>
    )
}

export default Header